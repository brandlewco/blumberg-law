[build]
  command = "npm run build"
  publish = "out"
  functions = ".netlify/functions"

[[plugins]]
package = "@netlify/plugin-sitemap"

  [plugins.inputs]
  exclude = [
    '404.html',
    'not-found.html',
    'cases/**.html'
  ]