[build]
  command = "npm run build"
  publish = "out"
  functions = ".netlify/functions"

[[plugins]]
package = "@netlify/plugin-sitemap"

  [plugins.inputs]
  buildDir = "public"
  exclude = [
    # By Glob pattern
    'cases/**.md'
  ]